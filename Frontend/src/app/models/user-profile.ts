export interface UserProfile {
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
  location: string;
  birthDate: string; // ISO formatta (yyyy-MM-dd)
  identityNumber: string;
  userNumber:string;
}
